name: Deploy to GitHub Pages
on:
  push:
    branches: [main]
  workflow_dispatch:
  schedule:
    - cron: '30 10 * * 1-5'
    - cron: '30 22 * * 0-4'
permissions:
  contents: read
  pages: write
  id-token: write
concurrency:
  group: pages
  cancel-in-progress: true
jobs:
  deploy:
    timeout-minutes: 15
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Regression checks
        run: |
          node --test tests/core.test.cjs tests/storage.test.cjs tests/quotes.test.cjs
          node --check quotes.js
          node --check app.js
          node --check sw.js
          node --check scripts/update-quotes.cjs
      - name: Update public market quotes
        run: node scripts/update-quotes.cjs
      - name: Configure Pages
        uses: actions/configure-pages@v5
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: .
      - name: Deploy
        id: deployment
        uses: actions/deploy-pages@v4
